{
  "name": "done-gate",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "description": "Evidence-enforced definition of done for Claude Code",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "reinstall": "claude plugin uninstall done-gate@done-gate --scope user; claude plugin install done-gate@done-gate --scope user"
  },
  "engines": {
    "node": ">=18"
  }
}
